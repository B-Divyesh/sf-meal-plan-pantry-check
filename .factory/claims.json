[
  {
    "id": "source-aware-list",
    "claim": "Scales recipe amounts, subtracts only pantry items you confirm, and keeps source arithmetic.",
    "where": "app first screen and README",
    "test": "npm run test:e2e -- --grep @claim:source-aware-list",
    "sandbox": "fresh browser context at /demo; add a recipe, double its servings, confirm one pantry item, and inspect the numeric total and source contribution"
  },
  {
    "id": "ingredient-parsing",
    "claim": "Normalizes compatible weight and volume units and visibly marks uncertain ingredient amounts.",
    "where": "How it works, recipe form, and README",
    "test": "npm run test:e2e -- --grep @claim:ingredient-parsing",
    "sandbox": "fresh browser context; enter compatible cup/milliliter and kilogram/gram lines plus an uncertain line, then inspect parsed and consolidated output"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "first-screen facts, app status, offline page, manifest, and README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "dedicated fresh browser context at /demo; wait for the service worker, go offline, reload, then close only that context"
  },
  {
    "id": "csv-export",
    "claim": "Exports the complete visible shopping list as CSV.",
    "where": "shopping list and README",
    "test": "npm run test:e2e -- --grep @claim:csv-export",
    "sandbox": "fresh browser context at /demo; compare the downloaded header and row count with visible shopping rows"
  },
  {
    "id": "clipboard-export",
    "claim": "Copies a grouped shopping list with quantities and recipe names.",
    "where": "shopping list and README",
    "test": "npm run test:e2e -- --grep @claim:clipboard-export",
    "sandbox": "fresh browser context at /demo with a recorded clipboard implementation; assert grouped content, quantity, and recipe source"
  },
  {
    "id": "print-checklist",
    "claim": "Prints the populated shopping checklist without app controls.",
    "where": "shopping list and README",
    "test": "npm run test:e2e -- --grep @claim:print-checklist",
    "sandbox": "fresh browser context at /demo; capture the print call, inspect populated text, and emulate print media"
  },
  {
    "id": "json-backup",
    "claim": "Exports and imports a complete JSON backup of the planner data.",
    "where": "data panel, privacy page, and README",
    "test": "npm run test:e2e -- --grep @claim:json-backup",
    "sandbox": "fresh browser context at /demo; change servings and pantry state, export, reset, import, and compare restored data"
  },
  {
    "id": "pwa-install",
    "claim": "Includes an installable PWA manifest and controlling service worker.",
    "where": "manifest and README",
    "test": "npm run test:e2e -- --grep @claim:pwa-install",
    "sandbox": "fresh browser context at /demo; validate standalone manifest fields, icons, and active controlling worker"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo changes are not saved to or mixed with real planner data.",
    "where": "demo banner, README, and .factory/demo.md",
    "test": "npm run test:e2e -- --grep @claim:demo-isolation",
    "sandbox": "save a real recipe, enter /demo, mutate and reset sample data, then return to the unchanged real planner while recording requests"
  },
  {
    "id": "local-privacy",
    "claim": "Recipes and planning state stay in IndexedDB and make no third-party request during normal planning.",
    "where": "first-screen facts, data panel, privacy page, footer, and README",
    "test": "npm run test:e2e -- --grep @claim:local-privacy",
    "sandbox": "fresh real-ledger context; record every request, save and reload a recipe, then inspect IndexedDB"
  },
  {
    "id": "localstorage-scope",
    "claim": "Only the license token and dated license verdict use application localStorage keys.",
    "where": "privacy page and README",
    "test": "npm run test:e2e -- --grep @claim:localstorage-scope",
    "sandbox": "fresh browser context with a recorded valid verification response; save a recipe and inspect every localStorage key"
  },
  {
    "id": "daily-license-check",
    "claim": "A saved license is verified at most once per day.",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:daily-license-check",
    "sandbox": "fresh browser context with a counted verification fixture; reload inside and beyond the 24-hour cache window"
  },
  {
    "id": "edition-limits",
    "claim": "The free edition stores four recipes; the $9 one-time Household Edition allows unlimited recipes with a valid license.",
    "where": "first-screen facts, Household Edition panel, README, and terms",
    "test": "npm run test:e2e -- --grep @claim:edition-limits",
    "sandbox": "fresh free and licensed contexts; assert the cap, live 303 checkout boundary and hosted $9 offer, then a recorded valid verification response and five saved recipes"
  }
]
